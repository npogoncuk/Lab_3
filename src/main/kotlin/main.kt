import comb.factorial
import comb.generateCombinations
import comb.generatePermutation
import comb.printAllPermutation

fun main(args: Array<String>) {
    val array: Array<Int> = arrayOf(1,2,3,4,5)
    printAllPermutation(array)
}
